<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="koffi.simplifiedcodingalpha.com.jonadanapaul">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/alpha_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/alpha_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SplashAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.SignInAct"
            android:label="" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.ProjektInit1von4Act" />
        <activity android:name=".activities.ProjektInit2von4Act" />
        <activity android:name=".activities.ProjektInit3von4Act"></activity>
    </application>

</manifest>